package com.videxedge.anima03;

import android.graphics.Bitmap;

public class Net extends Figure {
    public Net(int x, int y, Bitmap bitmap, int w, int h) {
        super(x, y, bitmap, w, h);
    }
}
